#!/usr/bin/env bash

. "./init-script"

echo "Running gulp for ${project_name^^} project..."
docker run \
    -it \
    --rm \
    --name gulp \
    -v ${PWD}/../../frontend:/root/fastresume/frontend \
    fastresume_node gulp "$@"
