#!/usr/bin/env bash

. "./init-script"

echo "Running yarn for ${project_name^^} project..."
docker run \
    -it \
    --rm \
    --name yarn \
    -v ${PWD}/../../frontend:/root/fastresume/frontend \
    fastresume_node yarn "$@"
